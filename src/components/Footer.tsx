const Footer = () => (
  <footer className="relative z-10 text-center py-8 border-t border-primary/30">
    <p className="font-body text-sm text-muted-foreground italic">
      "Not all power is shown. Some is controlled."
    </p>
  </footer>
);

export default Footer;
